# ==============================================================================
# DeepL Translation Frontend - Environment Configuration
# ==============================================================================
#
# This file contains configuration settings for the DeepL translation service.
# Copy this file to 'deepl.env' and adjust the values for your installation.
#
# IMPORTANT: Never commit the actual deepl.env file with real credentials to
# version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# DeepL API Configuration
# ------------------------------------------------------------------------------

# Your DeepL API authentication key (required)
# Get your key from: https://www.deepl.com/pro-api
DEEPL_AUTHKEY=your-deepl-api-key-here

# ------------------------------------------------------------------------------
# Django Security Settings
# ------------------------------------------------------------------------------

# Django secret key (AUTOMATICALLY GENERATED - DO NOT SET MANUALLY)
# The secret key is automatically generated on first run and stored in a file
# at the path specified by SECRET_KEY_FILE (default: /app/secrets/django_secret_key)
# This file is persisted in a Docker volume.
#
# For local development outside Docker, you can generate a key with:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# and set it here as DJANGO_SECRET_KEY=your-generated-key
#
# DJANGO_SECRET_KEY is set automatically by entrypoint.sh - do not set it here

# Debug mode (set to False in production)
DJANGO_DEBUG=False

# Allowed hosts (comma-separated list of domains/IPs allowed to access the service)
# Example: DJANGO_ALLOWED_HOSTS=deepl.example.com,192.168.1.100
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# CSRF trusted origins (comma-separated, required for HTTPS deployments)
# Include the full URL with protocol (https://), no trailing slash
# Example: CSRF_TRUSTED_ORIGINS=https://translation.jade.local,https://deepl.example.com
CSRF_TRUSTED_ORIGINS=https://translation.jade.local

# ------------------------------------------------------------------------------
# Application Branding
# ------------------------------------------------------------------------------

# Application title displayed in the browser tab
# Add a line for each language with the suffix _<LANG> (uppercase 2-letter code).
# The app picks the right value for the currently active UI language.
# Fallback order: current language → LANGUAGE_CODE → English → first available.
# Extend with more languages as needed, e.g. APP_TITLE_FR, APP_TITLE_ES, …
APP_TITLE_EN=DeepL Translation Frontend
APP_TITLE_DE=DeepL Übersetzungs-Frontend

# Organization or institution name (displayed in footer)
# Leave empty to hide organization name
# Same language-suffix convention as APP_TITLE above.
ORGANIZATION_NAME_EN=Your Organization Name
ORGANIZATION_NAME_DE=Ihr Organisationsname

# Footer text (displayed below organization name)
# Use {year} as placeholder for current year
# Leave empty to use default text
FOOTER_TEXT={year}+ Translation Service

# Logo filename (must be placed in staticfiles/logo/ directory)
# Supported formats: .jpg, .png, .svg
# Leave empty to disable logo
# Example: LOGO_FILENAME=logo.png
LOGO_FILENAME=

# Logo maximum width in pixels
# Default: 250
LOGO_MAX_WIDTH=250

# ------------------------------------------------------------------------------
# SSO/Proxy Logout Configuration
# ------------------------------------------------------------------------------

# SSO Logout URL (optional)
# When this application is deployed behind an SSO proxy (e.g., OAuth2-proxy,
# Keycloak gatekeeper), set this to the logout URL of your authentication proxy.
# This enables a "logout" link in the footer for users to sign out of the SSO.
#
# The application itself has no authentication - it relies on the upstream
# proxy for access control. This setting simply provides a convenient way
# for users to trigger a logout at the proxy level.
#
# Examples:
#   OAuth2-proxy: SSO_LOGOUT_URL=https://auth.example.com/oauth2/sign_out
#   Keycloak: SSO_LOGOUT_URL=https://auth.example.com/realms/myrealm/protocol/openid-connect/logout
#
# Leave empty or unset to hide the logout button
# SSO_LOGOUT_URL=

# ------------------------------------------------------------------------------
# Corporate Identity - Colors (hex codes WITHOUT the # symbol)
# ------------------------------------------------------------------------------

# Primary brand color (used for buttons, links, accents)
# Example: PRIMARY_COLOR=0d6efd (Bootstrap blue)
PRIMARY_COLOR=0d6efd

# Secondary brand color (used for secondary buttons and accents)
# Example: SECONDARY_COLOR=6610f2 (Bootstrap indigo)
SECONDARY_COLOR=6610f2

# ------------------------------------------------------------------------------
# Logging Configuration (Optional)
# ------------------------------------------------------------------------------

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file location
# LOG_FILE=/var/log/deepl-frontend/app.log

# ------------------------------------------------------------------------------
# Translation Settings
# ------------------------------------------------------------------------------

# DeepL translation model type
# Controls the quality/speed trade-off for translations.
# Allowed values:
#   latency_optimized        - classic models, preserves formatting (default)
#   prefer_quality_optimized - next-gen models, better quality but may break
#                              formatting (line breaks / whitespace are lost
#                              because next-gen models ignore split_sentences)
#   quality_optimized        - always use next-gen models (same caveat)
#   (empty)                  - use the DeepL API default
#
# WARNING: quality_optimized / prefer_quality_optimized use DeepL's next-gen
# models which force split_sentences=0 and therefore strip line breaks and
# whitespace structure from the translated output.  Use these only if
# formatting fidelity is not important for your use case.
DEEPL_MODEL_TYPE_TRANSLATION=latency_optimized

# Number of characters used for auto-detection
TRANSLATION_DETECTION_LEN=30

# Maximum length of source text per translation request (in characters).
# Set to 0 to disable the limit (default).  When set to a positive value,
# - the browser shows a live warning when the text exceeds the limit,
# - the submit button is disabled while the text is too long,
# - form validation on the server side rejects the request.
# Example: MAX_TRANSLATION_LENGTH=100000
MAX_TRANSLATION_LENGTH=0

# Controls access to document translation (Word .docx / PowerPoint .pptx).
# Three modes:
#   False      – feature hidden for everyone (default)
#   True       – feature visible to all users
#   <regex>    – visible only to users whose email (from X-Remote-Email header
#                set by oauth2-proxy) matches the regex (case-insensitive)
#
# Regex examples:
#
#   All @university.org addresses:
#     DOCUMENT_TRANSLATION_ENABLED=@university\.org$
#
#   @university.org PLUS two specific individuals:
#     DOCUMENT_TRANSLATION_ENABLED=@university\.org$|^john\.doe@email\.test$|^anotherone@somemail\.example$
#
#   Everyone (any valid email address contains @):
#     DOCUMENT_TRANSLATION_ENABLED=@
#
#   Nobody via regex (prefer False, but if a regex is needed):
#     DOCUMENT_TRANSLATION_ENABLED=(?!x)x
#
# DOCUMENT_TRANSLATION_ENABLED=False

# Fair-use confirmation checkbox text shown above the document upload form.
# Add a line for each language with the suffix _<LANG> (uppercase 2-letter code).
# The app picks the right value for the currently active UI language.
# Fallback order: current language → English → first available.
#
# Customise these texts to reflect your organisation's acceptable-use policy.
# The defaults (used when these variables are unset) are generic and do not
# mention a specific institution.
#
# DOCUMENT_TRANSLATION_FAIR_USE_TEXT_EN=I confirm that I need this translation for a purpose in the context of study, teaching, research, or administration.
# DOCUMENT_TRANSLATION_FAIR_USE_TEXT_DE=Ich bestätige, dass ich diese Übersetzung für einen Zweck im Kontext von Studium, Lehre, Forschung oder Verwaltung benötige.

# Maximum document upload size in megabytes.  Files exceeding this limit are
# rejected both client-side (JavaScript) and server-side (form validation).
# Note: values above 50 MB may require matching updates to client_max_body_size
# in nginx.conf and any downstream edge/reverse proxy.
# Default: 50
# MAX_DOCUMENT_SIZE_MB=50


# Maximum wall-clock time (in seconds) for a single document translation job.
# When the limit is reached the background worker aborts and marks the job as
# failed.  Set to 0 to disable the timeout (not recommended in production).
# DOCUMENT_TRANSLATION_TIMEOUT=180

# Statistics retention periods
STATISTICS_DAYS=31
STATISTICS_MONTHS=24
STATISTICS_YEARS=5

# ------------------------------------------------------------------------------
# Time Zone and Language
# ------------------------------------------------------------------------------

# Time zone for the application
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIME_ZONE=Europe/Berlin

# Default language code
LANGUAGE_CODE=en-us
